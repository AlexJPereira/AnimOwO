import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    tests: {
        textAlign: 'center',
        textAlignVertical: 'center',
        height: 500,
        fontSize: 40
    }
})