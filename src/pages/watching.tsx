import React from 'react';
import { ScrollView, Text } from 'react-native'

export default function Watching() {
    return(
        <ScrollView>
            <Text>Supose that's my watching screen.</Text>
        </ScrollView>
    )
}