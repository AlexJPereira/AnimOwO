import React from 'react';
import { ScrollView, Text } from 'react-native'

export default function Plan() {
    return(
        <ScrollView>
            <Text>Supose that's my anime's plan to watch.</Text>
        </ScrollView>
    )
}