import React from 'react';
import { ScrollView, Text } from 'react-native'

export default function Favorite() {
    return(
        <ScrollView>
            <Text>Supose that's my favorite's tab.</Text>
        </ScrollView>
    )
}